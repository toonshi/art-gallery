import {Great_Vibes} from 'next/font/google';

/** Script face for the "Nick Arts" wordmark. Self-hosted by next/font at build time. */
export const brandFont = Great_Vibes({
  weight: '400',
  subsets: ['latin'],
  display: 'swap',
});
